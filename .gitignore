node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Replit environment state (agent transcripts, wasm snapshots, log DBs)
.local/
.config/
sedK6SoEg

# Secrets
.env
.env.*
!.env.example

# Database dumps — never commit (drizzle migrations stay tracked)
*.sql
!migrations/**/*.sql
